version: "3"
services:
  api:
    build:
      context: ./api
    image: ryanpage/playlistory-api
    ports:
      - 3000:3000
  web:
    build:
      context: ./web
    image: ryanpage/playlistory-web
    ports:
      - 80:80
